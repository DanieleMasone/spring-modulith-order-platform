/**
 * Pricing module that owns catalog prices and produces validated quotes for order placement.
 */
@org.springframework.modulith.ApplicationModule(allowedDependencies = {})
package com.example.orderplatform.pricing;
